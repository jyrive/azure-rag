{
  "name": "azure-rag-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync": "svelte-kit sync",
    "dev": "sh -c 'export PUBLIC_API_BASE_URL=${PUBLIC_API_BASE_URL:-http://localhost:8000} PUBLIC_APP_NAME=${PUBLIC_APP_NAME:-azure-rag}; npm run sync && vite dev'",
    "build": "sh -c 'export PUBLIC_API_BASE_URL=${PUBLIC_API_BASE_URL:-http://localhost:8000} PUBLIC_APP_NAME=${PUBLIC_APP_NAME:-azure-rag}; npm run sync && vite build'",
    "preview": "vite preview",
    "check": "sh -c 'export PUBLIC_API_BASE_URL=${PUBLIC_API_BASE_URL:-http://localhost:8000} PUBLIC_APP_NAME=${PUBLIC_APP_NAME:-azure-rag}; npm run sync && svelte-check --tsconfig ./tsconfig.json'"
  },
  "dependencies": {
    "@sveltejs/adapter-static": "^3.0.8"
  },
  "devDependencies": {
    "@sveltejs/kit": "^2.16.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@types/node": "^22.15.21",
    "svelte": "^5.0.0",
    "svelte-check": "^4.1.0",
    "typescript": "^5.8.0",
    "vite": "^6.0.0"
  }
}
